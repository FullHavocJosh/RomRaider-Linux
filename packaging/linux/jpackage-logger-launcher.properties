# Secondary jpackage launcher for RomRaider Logger.
# RomRaider ships a single jar whose mode is selected by a CLI flag (see
# scripts/run.sh), so this launcher reuses the main app's jar/main-class/
# java-options and just adds the -logger argument.
arguments=-logger
linux-shortcut=true
